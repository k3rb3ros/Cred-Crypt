#pragma once

#include <cstdint>

/* This file is used to track the version of CredCrypt that is later written into th
 *  header
 */

static constexpr uint64_t VERSION_MAJOR{0};
static constexpr uint64_t VERSION_MINOR{8};
static constexpr uint64_t VERSION_REVISION{0};
